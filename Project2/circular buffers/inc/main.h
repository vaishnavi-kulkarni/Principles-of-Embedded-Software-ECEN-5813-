#include <stdlib.h>
#include <stdio.h>
#include "../inc/ring.h"
int switch_var,max, break_var, i, buff_size, buff_num, x, l, breakflag;
char data, *removed_data;
ring_t *ptr[100];
ring_t *newptr[100];
